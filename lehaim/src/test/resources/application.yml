spring:
  liquibase:
    change-log: classpath:db.changelog/db.changelog-test.xml
    contexts: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

lehaim:
  report:
    exclude-radiation-therapy-days-before: 14
    exclude-radiation-therapy-days-after: 60